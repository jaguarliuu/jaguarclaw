# ========================================
# JaguarClaw 完整部署 docker-compose
# 包含：PostgreSQL + Backend + Frontend
# ========================================

version: '3.8'

services:
  # PostgreSQL + pgvector
  postgres:
    image: pgvector/pgvector:pg16
    container_name: jaguarclaw-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-jaguarclaw}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-jaguarclaw}
      POSTGRES_DB: ${POSTGRES_DB:-jaguarclaw}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U jaguarclaw"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - jaguarclaw-network

  # Backend (Java + Spring Boot)
  backend:
    image: jaguarclaw/backend:latest
    container_name: jaguarclaw-backend
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      # 数据库配置
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/jaguarclaw
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER:-jaguarclaw}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD:-jaguarclaw}
      # LLM 配置（必须提供）
      LLM_ENDPOINT: ${LLM_ENDPOINT}
      LLM_API_KEY: ${LLM_API_KEY}
      LLM_MODEL: ${LLM_MODEL:-deepseek-chat}
      # 工作目录
      TOOLS_WORKSPACE: /app/workspace
    volumes:
      - workspace_data:/app/workspace
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/actuator/health"]
      interval: 30s
      timeout: 10s
      start_period: 60s
      retries: 3
    networks:
      - jaguarclaw-network

  # Frontend (Vue 3 + Nginx)
  frontend:
    image: jaguarclaw/frontend:latest
    container_name: jaguarclaw-frontend
    restart: unless-stopped
    depends_on:
      - backend
    ports:
      - "${PORT:-80}:80"
    networks:
      - jaguarclaw-network

volumes:
  postgres_data:
  workspace_data:

networks:
  jaguarclaw-network:
    driver: bridge
